---
description: 积分目标管线（规则表 → partfrac → Risch 子集）
globs: giac-rs/crates/giac-calculus/src/integrate*.rs,giac-rs/crates/giac-calculus/src/risch/**,giac-rs/crates/giac-calculus/src/partfrac_integrate.rs,.doc/cas-long-term-vision.md
alwaysApply: false
---

# 积分目标管线

摘自 `.doc/cas-long-term-vision.md` §4；新积分逻辑应归入此管线，避免孤立启发式。

```
integrate 请求
  → 规则表（代数、三角、有理换元等）
  → partfrac / Hermite / Rothstein
  → risch() 兜底（真 Risch 子集）
  → NotElementary | Exact(expr)
```

## 原则

- **规则表优先**；Risch 是兜底，不是每条积分的捷径
- 上游对标：`intg.cc` + `risch.cc`（见 `.doc/module-division.md`）
- 缺口跟踪：`.doc/issues/GIAC-algorithm-gaps-open.md`、`.doc/phase4-issues.md`
- 验收：`giac_check_integrate` 全绿或 documented 等价偏离（`assert_equiv`）

与 limit 共用：**先审计写死规则**（`algorithm-before-patch`），禁止为单条 check 行加形状函数。
