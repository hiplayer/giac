---
description: 测试与 conformance 规范（test-timeout、assert_equiv）
globs: giac-rs/**,.doc/**
alwaysApply: false
---

# 测试与 Conformance

**crate 单测分层与双轨：** [`.doc/test-writing-spec.md`](../../.doc/test-writing-spec.md)；写/改 `#[test]` 时见 [giac-test-writing.mdc](giac-test-writing.mdc)。

## 默认跑法

giac-rs **800+** 测；全 workspace **用 `cargo test-timeout`**，不要习惯性裸跑 `cargo test --workspace`（挂死会阻塞整个套件）。

```bash
cd giac-rs
cargo test-timeout                    # nextest + 单测超时
cargo test -p giac-calculus foo       # 调试子集可用裸 cargo test
```

超时：`.config/nextest.toml`（默认 50s；Gruntz CK-INT-60/61 等 30s override）。

## 等价判定

字面 golden 不一致时，优先 **`assert_equiv(a, b, ctx)`**（`normal(a-b)==0`），见 `.doc/conformance-testing.md` §3。

| API | 策略 |
|-----|------|
| `integrate` | `diff` 还原被积函数，或 `assert_equiv` |
| `solve` | 解集代入原方程 |
| `factor` | 展开乘积等于原式 |
| `normal` | 默认 `assert_equiv` |

## Golden 归一化

- `giac_check_cas`：浮点 **10 位有效数字** `%.10g`
- `giac_check_geo`：不稳定绘图 ID → `[ID]` / `,L,`
- 失败流程：字面 diff → `assert_equiv` → 查 `known-divergences.md`

## MVP 门禁

1. `cargo test-timeout` 全绿  
2. `./scripts/ci-clippy.sh` 全绿（clippy + `lint-substring-golden`）
3. golden 全绿 **或** 偏离已登记并验证

## 禁止 substring golden（`assert!` + `.contains("…")`）

Clippy `disallowed-methods` **无法**在 MSRV 1.75 上可靠匹配 `str::contains` 的 def_path（`Vec::contains` / `Token` 检查不受影响）。

```bash
cd giac-rs
./scripts/lint-substring-golden.sh   # 已并入 ./scripts/ci-clippy.sh
```

改用 `assert_equiv`、精确 `assert_eq!(format_expr(...), "...")` 或 `test_verify` helper。跟踪 [GIAC-expr-api-test-contains-cleanup.md](../../.doc/issues/GIAC-expr-api-test-contains-cleanup.md)。

## L1 失败（须人工确认）

Conformance SymPy 属性测（`giac_check_*`）失败时：**不得**为绿 CI 改 `sympy_verify.py` 或删用例。先查因 → 能修则改实现（PR 引用 [契约表](.doc/conformance-testing.md#35-命令-io-契约l1-normative)）→ 暂不能修则 `#[ignore = "L1-*"]` + `smoke-until` + [§8.1 登记](.doc/issues/GIAC-expr-api-test-contains-cleanup.md#81-l1-conformance-阻塞l1-)。详见 [conformance-testing.md §3.6](.doc/conformance-testing.md#36-l1-失败处理须人工确认)。
