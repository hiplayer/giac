---
description: limit 引擎四层管线与 exp 差分设计原则 P1–P3
globs: giac-rs/crates/giac-calculus/src/limit_engine/**,.doc/issues/GIAC-limit*.md
alwaysApply: false
---

# Limit 引擎架构（normative）

四层分工（`.doc/issues/GIAC-limit-layered-pipeline.md`）：

```
x 预处理 → 极限入口 → MRV 换元 → w 层级数 / remove_lnexp
```

新 gruntz 用例应 **串进管线**，而非旁路再加 `try_*` 形状函数。

## P1 — 同一理论，两层规则

`factor_exp_shifted_difference`（x 层）与 `remove_lnexp`（w 层）是 **同一 Gruntz/MRV 理论**；须收敛为共享规则表 / 同一递归遍历，不得各维护独立特例。

| 层 | 函数 | 目标形式 |
|----|------|----------|
| x | `factor_exp_shifted_difference` | `exp(主导) * (exp(ε) - 1)` |
| w | `remove_lnexp` | `w^{-1} * (exp(ε) - 1)` |

## P2 — 快路径有保质期

`limit_factored_exp_growth_at_infinity` 是 **临时**快路径；MRV 主路径收敛后删除或仅 `#[cfg(test)]`。禁止扩展该模式覆盖新用例。

## P3 — 禁止 per-gruntz 形状表

**不得**新增 `limit_gruntz_*`、`limit_nested_exp_diff_*` 等按用例命名函数。

允许扩展 **仅**：
1. `remove_lnexp` 规则表（w 层）
2. `preprocess` 递归因子分解（x 层，`factor_exp_shifted_difference` 泛化）

x 层 **禁止** 无公共项的 `exp(A)-exp(B)` fold（由 w 层 / MRV 处理）。

## 关键约束

不能对 `exp` 因子 **分别取极限**（`lim(exp(L))*lim(exp(S)-1)` 会错）；须代数合并后再取极限。
