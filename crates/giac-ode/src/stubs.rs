//! Legacy module kept for crate layout compatibility (`desolve` lives in `desolve.rs`).
//!
//! **API inventory:** inline `/// **Tier**` / `// **Tier**` on every function;
//! full module index in `.doc/giac-ode-api-stability.md`.
//!
