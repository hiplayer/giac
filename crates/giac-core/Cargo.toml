[package]
name = "giac-core"
description = "Core expression types and evaluation for giac-rs"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lints]
workspace = true

[features]
default = ["tower-common"]
## T4a: compositum via tower adjoin instead of flatten primitive-element search.
## Bisect / rollback: `cargo … -p giac-core --no-default-features` → Phase 0 flatten only.
tower-common = []

[dependencies]
giac-error = { workspace = true }
num-bigint = { workspace = true }
num-integer = { workspace = true }
num-rational = { workspace = true }
num-traits = { workspace = true }
thiserror = { workspace = true }
giac-poly = { workspace = true }
giac-groebner = { workspace = true }

[dev-dependencies]
serial_test = { workspace = true }
giac-simplify = { workspace = true }
